# ── Secrets (NEVER commit) ──────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Python ──────────────────────────────────────────────────────────────────
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ── Runtime / personal data (NOT for a public repo) ─────────────────────────
logs/
data/*.db
data/*.db-wal
data/*.db-shm
data/*.json
data/queue_state.json
data/conversations/
data/fastembed/
data/browser_*.png
# skill registry: builtins re-seed from code; installed skills are machine-local
data/skills/
memory/long_term.json
**/*.log

# ── Local tooling ───────────────────────────────────────────────────────────
.claude/

# ── Design source bundles (Figma Make exports — reference only) ──────────────
Web Chat Interface Design/

# ── OS ──────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
