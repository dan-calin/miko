# ── Hermes tool server ────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0

# ── Core AI ──────────────────────────────────────────────────────────────────
google-genai>=1.5.0
openai>=1.40.0
anthropic>=0.40.0
python-dotenv>=1.0.0

# ── Audio I/O ─────────────────────────────────────────────────────────────────
sounddevice>=0.4.7
numpy>=1.26.0

# ── Windows media / volume ────────────────────────────────────────────────────
pycaw>=20220427
comtypes>=1.4.7
pyautogui>=0.9.54
pyperclip>=1.9.0

# ── Discord + voice ───────────────────────────────────────────────────────────
discord.py[voice]>=2.4.0
PyNaCl>=1.5.0
imageio-ffmpeg>=0.5.1
edge-tts>=6.1.12

# ── YouTube extraction ────────────────────────────────────────────────────────
yt-dlp>=2024.11.4

# ── Web search ────────────────────────────────────────────────────────────────
ddgs>=0.1.0
requests>=2.32.0
beautifulsoup4>=4.12.0

# ── Semantic memory / second brain (local embeddings; optional but recommended) ─
# Without it, Miko falls back to Gemini/OpenAI embedding APIs, then keyword search.
fastembed>=0.3.0

# ── System info ───────────────────────────────────────────────────────────────
psutil>=6.0.0

# ── File operations ───────────────────────────────────────────────────────────
send2trash>=1.8.3

# ── Screenshot ────────────────────────────────────────────────────────────────
Pillow>=10.4.0
mss>=9.0.2

# ── Windows notifications ─────────────────────────────────────────────────────
win10toast>=0.9

# ── File system monitoring ────────────────────────────────────────────────────
watchdog>=4.0.2

# ── Calendar integration ──────────────────────────────────────────────────────
caldav>=1.3.0
icalendar>=5.0.0
msal>=1.26.0

# ── Personal Discord account control (local RPC) ──────────────────────────────
pypresence>=4.3.0

# ── Fuzzy matching (for file search) ─────────────────────────────────────────
rapidfuzz>=3.9.0

# ── Browser automation (optional — pip install playwright && playwright install chromium) ─
playwright>=1.40.0
